# js-rgb

A zero-dependency RGB color visualizer built with the HTML5 Canvas API. Generates random red, green, and blue color components every 300 ms and displays each channel alongside the combined result.

**Live demo:** <https://denysobukh.github.io/js-rgb/>

## Usage

Open `index.html` in any modern browser. Press **Space** to pause/resume the animation.
